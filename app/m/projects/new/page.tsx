import { MobileNewProjectScreen } from "@/components/mobile/new-project-screen"

export const metadata = {
  title: "New project — u did dat",
}

export default function MobileNewProjectPage() {
  return <MobileNewProjectScreen />
}
